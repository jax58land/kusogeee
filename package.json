{
  "name": "kusogame-oshisuna",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=iife --sourcemap --target=es2020",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=iife --sourcemap --target=es2020 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
